machine:
  timezone:
    America/Los_Angeles
  environment:
    PYTHONPATH: /home/ubuntu/ISB-CGC-WebApp:/home/ubuntu/ISB-CGC-WebApp/lib
  python:
    version: 2.7.10
dependencies:
  override:
    - sudo /home/ubuntu/ISB-CGC-WebApp/shell/install-deps.sh
database:
  post:
    - mysql -u ubuntu -e "USE mysql; SET PASSWORD FOR 'ubuntu'@'localhost' = PASSWORD('isb')"
    - sudo /home/ubuntu/ISB-CGC-WebApp/shell/database-setup.sh